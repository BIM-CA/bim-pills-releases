<Window x:Class="BIMPills.UI.Support.SupportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="BIM Pills — Soporte"
        Width="440"
        Height="620"
        MinWidth="380"
        MinHeight="500"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Loaded="Window_Loaded">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BIMPills.UI;component/Shared/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource PrimaryBrush}"
                Padding="20,14,20,14">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="/BIMPills.UI;component/Resources/pill-icon.png"
                       Width="22" Height="35"
                       Margin="0,0,12,0"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       VerticalAlignment="Center"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Soporte BIM Pills"
                               FontSize="15" FontWeight="SemiBold"
                               Foreground="White"/>
                    <TextBlock Text="¿Cómo podemos ayudarte?"
                               FontSize="11"
                               Foreground="#CCFFFFFF"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- WebView2 / Fallback -->
        <Grid Grid.Row="1">

            <!-- WebView2 (visible cuando está disponible) -->
            <wv2:WebView2 x:Name="WebView"
                          Visibility="Collapsed"/>

            <!-- Loading overlay -->
            <StackPanel x:Name="LoadingPanel"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                <TextBlock Text="Cargando soporte..."
                           FontSize="13"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Fallback: WebView2 Runtime no instalado -->
            <StackPanel x:Name="FallbackPanel"
                        Visibility="Collapsed"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Margin="32">
                <TextBlock Text="⚠️"
                           FontSize="36"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,12"/>
                <TextBlock Text="Se requiere Microsoft WebView2 Runtime"
                           FontSize="14" FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"/>
                <TextBlock x:Name="FallbackSubtitle"
                           Text="Para abrir el chat de soporte, instala WebView2 Runtime (gratuito, de Microsoft)."
                           FontSize="12"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,8,0,20"/>
                <Button Content="Descargar WebView2 Runtime"
                        Style="{StaticResource PrimaryButton}"
                        Click="DownloadWebView2_Click"
                        HorizontalAlignment="Center"/>
            </StackPanel>

        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="16,10,16,10">
            <Grid>
                <TextBlock x:Name="StatusText"
                           Text="Conectando con el equipo de BIM-CA..."
                           FontSize="11"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           VerticalAlignment="Center"/>
                <Button Content="Cerrar"
                        Style="{StaticResource SecondaryButton}"
                        HorizontalAlignment="Right"
                        Width="80" Height="28"
                        Click="Close_Click"/>
            </Grid>
        </Border>

    </Grid>
</Window>
